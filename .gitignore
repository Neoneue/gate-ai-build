# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Claude Code state
.claude/settings.local.json
.claude/worktrees/
.tmp/
.tmp-screenshots/

# Vendored agent bundle (sourced separately)
front-end-developer/

# Local-only project notes (not shipped). The whole docs/ folder stays local;
# the committed UI change logs live in change-logs/ (tracked) instead.
docs/
gateway-fixes.md
# Anchored: the private resume scratchpad is the ROOT handoff.md only. Without
# the leading slash this pattern matched at any depth and silently ignored
# .claude/commands/handoff.md (the /handoff command), which must stay tracked.
/handoff.md
.rams-capture/
.playwright-mcp/

# Editor workspace files
*.code-workspace
