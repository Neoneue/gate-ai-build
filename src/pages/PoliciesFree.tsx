import { Policies } from "@/pages/Policies";

/** Free-tier twin of Policies. Verbatim Pro page for now; diverge here (e.g.
 *  show PRO options disabled instead of unlocked). */
export function PoliciesFree() {
  return <Policies />;
}
