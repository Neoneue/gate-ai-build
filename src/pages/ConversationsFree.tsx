import { Conversations } from "@/pages/Conversations";

/** Free-tier twin of Conversations. Verbatim Pro page for now; diverge here. */
export function ConversationsFree() {
  return <Conversations />;
}
