import { ConversationsFree } from "@/pages/ConversationsFree";

/** Default-workspace twin of Conversations. Uses Free content; diverge here. */
export function ConversationsDefault() {
  return <ConversationsFree />;
}
